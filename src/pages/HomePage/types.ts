export type PositionType = {
  latitude: number;
  longitude: number;
};

export type CategoryDatas = {
  id: string;
  title: string;
  pic: string;
  alt: string;
};

export type TasteTheNewDatas = {
  id: string;
  description: string;
  price: string;
  alt: string;
  pic: string;
};
